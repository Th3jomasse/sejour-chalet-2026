rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Le séjour partagé : lecture/écriture réservée aux utilisateurs connectés
    // (connexion anonyme automatique dans l'app).
    match /sejours/{sejour} {
      allow read, write: if request.auth != null;
    }
  }
}
